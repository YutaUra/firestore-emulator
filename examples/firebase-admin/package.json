{
  "name": "@example/firebase-admin",
  "version": "0.0.52",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"pnpm exec jest\"",
    "test:watch": "firebase emulators:exec --only firestore \"pnpm exec jest --watch\"",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "type:check": "tsc --noEmit"
  },
  "dependencies": {
    "@firestore-emulator/jest": "workspace:*",
    "date-fns": "4.1.0",
    "firebase-admin": "12.5.0",
    "firebase-tools": "13.18.0"
  },
  "devDependencies": {
    "@shared/biome-config": "workspace:^",
    "@shared/typescript-config": "workspace:^",
    "@types/jest": "29.5.13",
    "@types/node": "20.16.5",
    "jest": "29.7.0",
    "ts-jest": "29.2.5",
    "typescript": "5.6.2"
  }
}
