{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "outputs": ["dist"],
      "inputs": ["tsconfig.json", "vite.config.ts", "package.json", "src/**/*"]
    }
  }
}
