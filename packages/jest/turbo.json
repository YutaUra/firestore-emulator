{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "outputs": ["dist"],
      "inputs": ["vite.config.ts", "package.json", "src/**/*"]
    }
  }
}
